{
  "name": "remember-pages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.js",
    "test": "node scripts/test.js",
    "dryrun": "npx wrangler deploy --dry-run --env production"
  }
}
